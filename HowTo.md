Please use python version 3.7

Firstly install dependencies via:
pip install -r requirements.txt

To run the diffusion parameter results viewer please run:
fbs run

To create executable:
fbs freeze

To create installer create executable and then:
fbs installer